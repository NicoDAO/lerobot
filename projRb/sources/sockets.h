#ifndef SOCKETS_H
#define SOCKETS_H

#include <sys/types.h> /* See NOTES */
#include <sys/socket.h>

class sockets
{
    public:
        sockets();
        virtual ~sockets();

    protected:

    private:
};

#endif // SOCKETS_H
